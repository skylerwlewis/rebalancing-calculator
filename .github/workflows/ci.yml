name: CI

on:
  pull_request:

permissions:
  pull-requests: write
  contents: read

jobs:
  unit-tests:
    uses: skylerwlewis/test-actions/.github/workflows/unit-tests-vitest.yml@v1

  e2e:
    uses: skylerwlewis/test-actions/.github/workflows/e2e-ci.yml@v1
