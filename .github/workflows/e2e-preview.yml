name: E2E Tests (Cloudflare Preview)

on:
  pull_request:

permissions:
  issues: write
  pull-requests: write
  contents: read

jobs:
  e2e-preview:
    uses: skylerwlewis/test-actions/.github/workflows/e2e-cloudflare-preview.yml@main
